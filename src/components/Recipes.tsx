import React from 'react';

const Recipes:React.FunctionComponent = (): React.ReactElement => {
    return (
        <div>
            <h1>Recipes</h1>
        </div>
    );
}

export default Recipes;